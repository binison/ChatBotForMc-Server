# Support

如果你在使用 `ChatBotForMc` 时遇到问题，建议按以下顺序处理：

1. 先查看 `README.md` 中的配置示例与排障章节。
2. 确认你修改的是运行时配置文件：`plugins/ChatBotForMc/config.yml`。
3. 打开 `debug: true`，执行 `/ai reload` 后重试，并查看服务端日志。
4. 如果确认是代码问题或缺陷，再到仓库里提交 Issue。

建议在反馈问题时附带：
- Paper 版本
- Java 版本
- 插件版本
- 使用的模型服务商
- 关键错误日志

